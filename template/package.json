{
  "name": "{{projectName}}",
  "version": "{{projectVersion}}",
  "description": "{{projectDescription}}",
  "author": "Roy Luo",
  "private": false,
  "scripts": {
    "test": "npm run unit",
    "start": "parcel index.html"
  }
}